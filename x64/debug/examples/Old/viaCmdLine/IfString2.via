// IfString2.lnl

application PE GUI;

import MessageBox ascii lib "USER32.DLL",4;

string message;
string messageII;

entry
	message = "Compare"; messageII = "Compare";
	if (message = messageII) {
		MessageBox(0,"Equal","Strings..",0);
	} else {
		MessageBox(0,"Not Equal","Strings..",0);
	}

end.